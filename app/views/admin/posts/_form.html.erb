<%= render 'error_messages' %>
<div class="field">
  <p><%= f.label :title, "Title" %></p>
  <p><%= f.text_field :title %></p>
</div>

<div class="field">
  <p><%= f.label :body, "Body" %></p>
  <p><%= f.text_area :body, :size => "20x40" %></p>
</div>

<p class="button"><%= f.submit "Post", :disable_with => 'Posting...' %></p>