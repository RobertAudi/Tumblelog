<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <%= stylesheet_link_tag "application", "blueprint/screen" %>
  <%= stylesheet_link_tag "blueprint/print", :media => "print" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header class="clearfix">
    <section>
    <nav>
      <p class="left"><%= link_to "Visit your site", '#' %></p>
      <p class="right">Logged in as <%= link_to "User", '#' %></p>
    </nav>
    </section>
  </header>

  <div class="container">
    <section>
      <% flash.each do |key, value| %>
        <div class="flash <%= key %>">
          <%= value %>
        </div>
      <% end %>

      <%= yield %>
    </section>

    <%= debug params if Rails.env.development? %>
  </div>
</body>
</html>
